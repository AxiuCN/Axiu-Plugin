/** 日志前缀 */
export const LOG_PREFIX = '[Axiu-Plugin]'

/** 签到模块日志前缀 */
export const SIGNIN_LOG_PREFIX = '[Axiu-Plugin][签到]'

/** 默认入群审核配置（Map 格式） */
export function getDefaultGroupConfig() {
  return new Map()
}
